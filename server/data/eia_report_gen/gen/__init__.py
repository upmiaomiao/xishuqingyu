# -*- coding: utf-8 -*-
"""环评报告生成智能体（先做污染影响类《报告表》）。

设计纪律（与审核线同源）：
  ① 结论由**代码**给：名录档级、专项评价设置、章节清单、标准清单都不许模型拍脑袋；
  ② 模型只写叙述，且叙述里的**每个数字必须来自填报表或判据库**，否则留空并标注"需人工"；
  ③ 填报表里没有的事实，一律不猜 —— 生成稿里写「需人工补充」，而不是编一个像样的值；
  ④ 同一份填报 → 同一份 Word（字节级可复现，docx 时间戳固定）；
  ⑤ 生成稿必须能过自家 18 项审核（生成→自审→回修闭环）。
"""

__all__ = ["schema", "rules", "decide", "render", "docx_writer", "selfaudit"]